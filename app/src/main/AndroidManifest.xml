<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.takedelivery">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TakeDelivery">
        <activity android:name=".CadastroEmpresa"></activity>
        <activity android:name=".Empresa" />
        <activity android:name=".Cliente1" />
        <activity android:name=".CadastroInicialEmpresa" />
        <activity android:name=".CadastroCliente" />
        <activity android:name=".AcessoEmpresa" />
        <activity android:name=".AcessoCliente" />
        <activity android:name=".Escolha" />
        <activity android:name=".Begin" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>